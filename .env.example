# ── MCPanel Configuration ──────────────────────────────────────
# Copy this file to .env and fill in your values
# Then run: bash SETUP.sh

# Dashboard port (exposed on the host machine)
PORT=3007

# Minecraft RCON connection
RCON_HOST=
RCON_PORT=25575
RCON_PASSWORD=

# Minecraft server (local network)
MC_LOCAL_HOST=
MC_LOCAL_PORT=25565

# Minecraft server (external / internet)
MC_EXT_HOST=
MC_EXT_PORT=25565
